query GetOrder($id: UUID!) {
  order(id: $id) {
    id
    status
    reference
    dateCreated
    items {
      totalCount
      nodes {
        id
        description
        price
        dateCreated
      }
    }
  }
}